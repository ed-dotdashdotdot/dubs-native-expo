import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  pageHeading: {
    fontSize: 32,
    fontWeight: 'bold',
    textAlign: 'center',
    width: '100%',
  }
});

export default styles;
